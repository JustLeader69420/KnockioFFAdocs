[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "knockioffa"
authors = [{name = "JustLeader", email = "justleader@justleader.eu"}, {name = "Onionar"}]
dynamic = ["version", "description"]
